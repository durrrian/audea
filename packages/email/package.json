{
  "name": "@repo/email",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "files": [
    "templates/",
    "render.ts",
    "index.ts"
  ],
  "scripts": {
    "dev": "email dev --port 8008 --dir templates",
    "clean": "rimraf node_modules && rimraf .react-email"
  },
  "dependencies": {
    "@react-email/components": "0.0.16",
    "@react-email/render": "^0.0.12",
    "@repo/api": "workspace:*",
    "@repo/helper": "workspace:*",
    "@repo/prisma": "workspace:*",
    "react": "^18.2.0",
    "react-email": "^2.1.1",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@repo/eslint-config": "workspace:*"
  }
}
