import type { Viewport } from 'next'

const viewport: Viewport = {
  themeColor: '#1F365F',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}

export default viewport
